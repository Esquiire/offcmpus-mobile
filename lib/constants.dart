import 'package:flutter/cupertino.dart';

class Constants {
  static Color pink({double opacity = 1}) =>
      Color.fromRGBO(224, 119, 125, opacity);

  static Color navy({double opacity = 1}) =>
      Color.fromRGBO(59, 67, 83, opacity);

  static Color grey({double opacity = 1}) =>
      Color.fromRGBO(204, 204, 204, opacity);
}
